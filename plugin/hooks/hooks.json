{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import subprocess, os; branch=subprocess.run(['git','branch','--show-current'],capture_output=True,text=True,cwd=os.getcwd()).stdout.strip() or 'unknown'; project=os.path.basename(os.getcwd()); print(f'harness2 — ready.\\nProject: {project}  Branch: {branch}\\nLoop: plan → develop → verify → document → close\\nAuto-routing on. Just describe what you want.')\"",
            "timeout": 5,
            "statusMessage": "harness2 ready"
          },
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/note_freshness.py --from-git 1 --quiet || true",
            "timeout": 5,
            "statusMessage": "Checking note freshness"
          },
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/contract_lint.py --quick --quiet || echo '[maintain-suggested] CONTRACTS.md drift — run maintain skill'",
            "timeout": 5,
            "statusMessage": "Checking contract drift"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/stop_gate.py",
            "timeout": 10,
            "statusMessage": "Checking open tasks"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/prewrite_gate.py",
            "timeout": 3,
            "statusMessage": "Checking write gate"
          }
        ]
      }
    ]
  }
}
