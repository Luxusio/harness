# Architecture boundary rules — added by architecture-guardrails skill when rules are confirmed.
# arch-rules.yaml is a FALLBACK for repos without native lint boundary rules configured.
# Native ESLint/ruff/go-vet rules take precedence when available.

boundaries: []
# Example:
# - name: "domain-no-db"
#   description: "Domain layer must not import database packages"
#   source: "src/domain/**"
#   forbidden_imports: ["src/db/**", "knex", "prisma"]
#   language: "typescript"
#
# - name: "api-no-internal"
#   description: "Public API must not import internal modules"
#   source: "src/api/**"
#   forbidden_imports: ["src/internal/**"]
#   language: "python"
