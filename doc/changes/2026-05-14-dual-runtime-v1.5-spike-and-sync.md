# 2026-05-14 — dual-runtime v1.5 spike and sync

**Task:** TASK__dual-runtime-v1.5-spike-and-sync

This task delivers the three v1 deferrals from TASK__dual-runtime-plugin-claude-codex: three SKILL.md hand-ports to Codex CLI form, a binding canonical-form decision grounded in measured porting friction, a sync engine MVP, and a Codex config emitter with hooks-block emission. Three skills were ported with per-skill portability measurements recorded in `doc/harness/spike-report.md` (setup: 71% mechanical-portable, run: 56%, plan: 45% with dual-voice premise gate degraded to single Codex prompt); the spike-report §3.5 decision row selects YAML/JSON intermediate as the canonical form, binding for all future sync work. The sync engine MVP at `plugin/runtime-sync/` consists of `canonical_schema.py` (field schema + TOOL_TOKEN_MAP as single source of truth for runtime-specific tool names) and `transform_skill.py` (reads a `.skill.yaml` canonical, emits both a Claude SKILL.md and a Codex SKILL.md via token substitution), validated by a golden corpus fixture and 9 passing tests. The MCP config emitter at `plugin/runtime-sync/emit_codex_config.py` extends v1 AC-005 to also emit a `[hooks]` block and a `[hooks.state.<key>].trusted_hash` table required for hooks to fire under Codex, with 7 passing tests; placeholder hashes are emitted by default and real trust state is acquired on first interactive `codex` run.
